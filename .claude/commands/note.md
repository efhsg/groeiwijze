---
allowed-tools: Read, Bash
description: Schrijf een AI Note in het lopende PromptManager-project via de runner-wrapper
label: AI Note schrijven
---

# Note

Volg het skill contract in `skills/note.md`.

## Task

$ARGUMENTS

Als `$ARGUMENTS` leeg is, leid titel en body af uit de huidige sessie-context. Stel geen vraag tenzij de context echt onvoldoende is.
